version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    # Trigger another immediate Dependabot re-check after publishing the post-review tag.
    schedule:
      interval: daily
    labels:
      - automerge
